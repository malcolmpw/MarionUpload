<UserControl x:Class="MarionUpload.Views.vwCadUnit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="Import CadUnits" Command="{Binding CommandImportCadUnits}"                                                     
                                Margin="10" Width="200" Height="30" HorizontalAlignment="Left"/>
                <Button Content="Upload CadUnits" Command="{Binding CommandUploadCadUnits}"                                                     
                                Margin="10" Width="200" Height="30" HorizontalAlignment="Left"/>
            </StackPanel>
            <DataGrid Name="dgCadUnits" Margin="10" Width="1400" Height="600" HorizontalAlignment="Left"
                      ItemsSource="{Binding CadUnits}">
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
