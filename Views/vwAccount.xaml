<UserControl x:Class="MarionUpload.Views.vwAccount"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid>
        <StackPanel >
            <StackPanel Orientation="Horizontal">
                <Button Content="Import Accounts" Command="{Binding CommandImportAccounts}"  IsEnabled="{Binding AccountImportEnabled}"                                                  
                                Margin="10" Width="200" Height="30" HorizontalAlignment="Left"/>
                <Button Content="Upload Accounts" Command="{Binding CommandUploadAccounts}"  IsEnabled="{Binding AccountUploadEnabled}"               
                                Margin="10" Width="200" Height="30" HorizontalAlignment="Left"/>
                <TextBlock Text="{Binding MarionAccounts.Count}" 
                                Margin="10" Width="200" Height="30" HorizontalAlignment="Left"/>
            </StackPanel>
            <DataGrid Name="dgAccounts" Margin="10" Width="1400" Height="700" HorizontalAlignment="Left"
                            ItemsSource="{Binding MarionAccounts}">
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
